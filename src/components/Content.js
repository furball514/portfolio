import React from 'react';
import { StyleSheet, css } from 'aphrodite';

const Content = () => <div className={css(styles.content)} />;

export default Content;

const styles = StyleSheet.create({});
